[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
jupyter = "*"

# pip install --ignore-installed pyspark==2.4.4
# pip install spark-nlp==2.6.4
# pip install numpy

[requires]
python_version = "3.7"
